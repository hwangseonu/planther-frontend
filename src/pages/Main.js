import React, {Component} from 'react';

import Navbar from '../components/Navbar';

class Main extends Component {
  render() {
    return (
      <div id={'main'}>
        <Navbar/>
      </div>
    )
  }
}

export default Main;